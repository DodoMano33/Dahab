
// Import the function from the new shared location
import { detectAnalysisType } from "./analysisConfigBuilder";

// Re-export the function to maintain backward compatibility
export { detectAnalysisType };
